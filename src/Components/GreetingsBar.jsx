import { View, Text } from "react-native";

const Greetings = ({greetins, firstname, lastname}) => {

    return (
        <View style={{
            flexDirection: ""
        }}>
            
        </View>
    )
}